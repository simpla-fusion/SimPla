SET(MOAB_DIR /pkg/sigma/1.2/lib)
SET(MeshKit_DIR /pkg/sigma/1.2/lib)
SET(OCCT_PREFER_COMMUNITY_EDITION ON)

FIND_PACKAGE(OCE 0.17 REQUIRED)
LINK_DIRECTORIES(${OpenCASCADE_LIBRARY_DIR})
MESSAGE("-- OCE Found:${OCE_LIBRARIES} INCLUDE:${OCE_INCLUDE_DIRS}")


FIND_PACKAGE(MOAB REQUIRED)
IF (MOAB_FOUND)
    MESSAGE(" -- MOAB Found: ${MOAB_LIBRARIES};  INCLUDE: ${MOAB_INCLUDE_DIRS}")
ENDIF (MOAB_FOUND)

FIND_PACKAGE(MeshKit REQUIRED)

FIND_PACKAGE(Qt5 REQUIRED COMPONENTS Core Gui X11Extras Widgets)

INCLUDE_DIRECTORIES(${MOAB_INCLUDE_DIRS} ${MESHKIT_INCLUDE_DIRS}
        ${OCE_INCLUDE_DIRS} ${Qt5Application_INCLUDE_DIRS} ${Qt5Widgets_INCLUDE_DIRS} ${Qt5X11Extras_INCLUDE_DIRS})

FILE(GLOB geqdsk_SRC ./*.cpp)
ADD_EXECUTABLE(geqdsk_convert geqdsk_convert.cpp)
TARGET_LINK_LIBRARIES(geqdsk_convert ${OCE_LIBRARIES} ${MOAB_LIBRARIES} Qt5::Core Qt5::Gui Qt5::Widgets concept toolbox data)


#ENDIF (BUILD_TOOLS)
