FIND_PACKAGE(SWIG)
FIND_PACKAGE(PythonLibs)
INCLUDE_DIRECTORIES(${PYTHON_INCLUDE_PATH})

include(UseSWIG)

SET_SOURCE_FILES_PROPERTIES(SimPla.i PROPERTIES CPLUSPLUS ON)

#SET_SOURCE_FILES_PROPERTIES(SimPla.i PROPERTIES SWIG_FLAGS "-includeall")

SET(CMAKE_SWIG_OUTDIR ${CMAKE_LIBRARY_OUTPUT_DIRECTORY})

SWIG_ADD_MODULE(SimPla python SimPla.i pythonTypeMap.h)

SWIG_LINK_LIBRARIES(SimPla  engine io fluid emfield utilities pic  
             ${Boost_LIBRARIES}  ${OpenMP_LIBRARIES})
