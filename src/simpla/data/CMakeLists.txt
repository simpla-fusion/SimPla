FILE(GLOB data_SRC ./*.cpp ./backend/*.cpp)
#FILE(GLOB data_SRC DataTable.cpp )
add_library(data ${data_SRC})
target_include_directories(data BEFORE PRIVATE ${HDF5_INCLUDE_DIRS} ${LUA_INCLUDE_DIR} ${MPI_C_INCLUDE_PATH})
target_link_libraries(data ${LUA_LIBRARIES} parallel toolbox  ${HDF5_LIBRARIES} ${MPI_LIBRARIES}  )