
FIND_PACKAGE(HDF5 1.8 COMPONENTS C HL REQUIRED)
IF (HDF5_FOUND)
    INCLUDE_DIRECTORIES(${HDF5_INCLUDE_DIRS})
ENDIF (HDF5_FOUND)

FIND_PACKAGE(Lua 5.2 REQUIRED)
IF (Lua_FOUND)
    INCLUDE_DIRECTORIES(${LUA_INCLUDE_DIR})
ENDIF (Lua_FOUND)

IF (NOT SAMRAI_DIR)
    SET(SAMRAI_DIR /pkg/SAMRAI/3.11.0-debug)
ENDIF (NOT SAMRAI_DIR)
FIND_PACKAGE(SAMRAI)
INCLUDE_DIRECTORIES(${SAMRAI_INCLUDE_DIRS})

FILE(GLOB data_backend_SRC ./*.cpp)

add_library(data_backend ${data_backend_SRC})
target_link_libraries(data_backend ${LUA_LIBRARIES} toolbox ${SAMRAI_LIBRARIES} ${HDF5_LIBRARIES} ${MPI_LIBRARIES} netcdf)