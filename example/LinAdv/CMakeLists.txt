INCLUDE_DIRECTORIES(${SAMRAI_INCLUDE_DIRS})
FILE(GLOB_RECURSE LinAdv_SRC *.C)
FILE(GLOB_RECURSE LinAdv_f_SRC fortran/*.f)
ADD_EXECUTABLE(LinAdv ${LinAdv_SRC} ${LinAdv_f_SRC})
TARGET_LINK_LIBRARIES(LinAdv ${SAMRAI_LIBRARIES} ${MPI_LIBRARIES} ${HDF5_LIBRARIES} ${HYPRE_LIBRARIES})
